import React from "react";
import { ActivityIndicator, Text } from "react-native";

const FooterItem = () => {
  return (
    <ActivityIndicator style={{flex: 1, alignItems: 'center', justifyContent: 'center', padding: 10, color: 'blue'}} />
  )
}

export default FooterItem;
